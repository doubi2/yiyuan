<view class="mysearch">
    <van-search
        background="#E8EAED"
        value="{{ value }}"
        placeholder="疾病 医生 科室 医院"
        use-action-slot
        bind:search="onSearch"
    >
  <view slot="action"  bind:tap="onSearch">取消</view>
</van-search>
</view>
